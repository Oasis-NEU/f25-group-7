
[build]
  base = "front-end"
  command = "npm run build"
  publish = "dist"

[context.production.environment]
# Ensure Netlify uses a modern Node version; `.nvmrc` is also provided.
  NODE_VERSION = "18"
